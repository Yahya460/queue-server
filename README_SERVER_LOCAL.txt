📡 نظام الدور الآلي — خادمي (Plus) لعمل شاشة العرض على الكمبيوتر وروابط للفاحصين على الجوال
التاريخ: 2025-08-10 12:57

الخطوات السريعة (ويندوز):
1) ثبّت Node.js.
2) داخل مجلد المشروع:
   npm install
   copy .env.sample .env   ← غيّر EXAM_CODE لو أردت
   npm start
3) شاشة العرض: http://localhost:3000/display   ← اضغط "تفعيل الصوت" مرة واحدة
4) روابط هواتف الفاحصين (في نفس الشبكة): http://localhost:3000/phones
   - سترى قائمة بروابط جاهزة مثل: http://IP:3000/exam لتشاركها مع الفاحصين.

تشغيل تلقائي عند تشغيل الجهاز:
• من مجلد launch_auto:
  - install_server_task.ps1        ← يشغّل الخادم (node server.js) تلقائيًا بصمت عند تسجيل الدخول.
  - open_firewall_3000.ps1         ← يفتح منفذ 3000 في جدار حماية ويندوز.
  - install_display_task.ps1       ← يفتح شاشة العرض http://localhost:3000/display بصمت بعد تسجيل الدخول.
  - launch_display_fullscreen_silent.vbs ← للتشغيل الصامت الفوري بدون نافذة.

ملاحظات أمان:
• EXAM_CODE هو الكود المشترك للفاحصين. احفظه بين الفريق فقط.
• للوصول من خارج الشبكة المحلية، استخدم اسم نطاق وHTTPS (عبر عكس وكيل مثل Nginx/Traefik) وافتح المنفذ على الراوتر.